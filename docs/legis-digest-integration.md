# Изменения в праве

Реализовано 22.09.2026 по запросу владельца: автоматическая ретрансляция опубликованных материалов [LegisDigest](https://t.me/LegisDigest) в `/changes`. Исходный проект: https://github.com/Bogolubov-creator/legis-digest. В первом импорте 16 справок и обзоров; отдельно сохранён архив из 65 актов за 30.06–06.07.2026.

## Чтение

По умолчанию открываются справки и обзоры: краткий фрагмент в списке и полный исходный текст в карточке. Сохраняются абзацы, подзаголовки и допустимые ссылки на источники. Новые пояснения сайт не генерирует. Автоматические материалы подписаны именем источника, проверка человеком не подтверждена; публикация в канале не означает одобрение юристом. Это выбранный владельцем режим ретрансляции, а не прежнее предложение редакционного согласования каждого текста.

Поиск работает по всему тексту. Тип материала, период и режим просмотра сохраняются в URL. Постоянный адрес `/changes/tg-9` открывает конкретную справку; исходный пост доступен по отдельной ссылке. Архив актов остаётся самостоятельным режимом. Неизвестные даты вступления в силу не вычисляются из текста обзора.

## Автоматическое обновление зеркала

Workflow `.github/workflows/deploy-pages.yml` в репозитории `club-pravo-hse-mirror` запускается каждый час на 17-й минуте UTC. GitHub может задерживать плановые задания. Перед сборкой он читает предыдущую опубликованную базу, получает открытую ленту `https://t.me/s/LegisDigest`, объединяет записи по идентификатору поста и публикует новую сборку Pages. Открытая страница проверяет JSON раз в минуту.

Команда из корня монорепозитория:

```bash
pnpm --filter @club/scripts exec tsx ../apps/web/scripts/sync-changes.ts --output ../apps/web/public/data/changes.json --previous-published
pnpm --filter @club/web build:mirror
```

Для внеочередного обновления запустить workflow `Deploy Pages mirror` с `source_ref=codex/v3-backlog-polish`. Импорт не требует Telegram-токена, доступа к подписчикам или новых LLM-вызовов. Обычная сборка сайта использует включённый JSON; ежечасный импорт настроен именно для Pages-зеркала.

## Сохранность и ограничения

Повторный импорт не создаёт дублей, доступные исправленные посты заменяют старую версию. Записи, вышедшие за окно ленты, сохраняются; исчезновение из окна не трактуется как удаление. История прежних редакций текста отдельно не хранится. Обход ограничен десятью страницами. Удалённые посты автоматически не удаляются из накопленного архива.

Импортер проверяет канал, даты, уникальность, размер и схему. Разметка разбирается инертно и выводится текстовыми React-узлами. Допустимы только HTTPS-ссылки из списка источников; HTTP-ссылки официального `publication.pravo.gov.ru` переводятся на HTTPS. Запись файла атомарная.

При ошибке ленты сохраняются прежние материалы и дата последней успешной загрузки, выставляется статус недоступности. Если нельзя получить предыдущую опубликованную базу, сборка останавливается, прежний сайт остаётся опубликованным. Нельзя подменять ошибку пустой базой.

Успешная загрузка и дата последнего поста показываются отдельно. На момент проверки последний открытый пост датирован 07.09.2026. Работа генератора на сервере не подтверждена: указанный SSH-сервер недоступен, ключ отсутствует на этом Mac. Интеграция забирает новые публикации, когда они появляются; она не восстанавливает сервер генерации. Это выборка источника, а не полная база российского законодательства.

## Проверка

Тесты проверяют реальный формат импорта, текст и ссылки, изоляцию сообщений, повтор/исправление постов, пагинацию, сохранение базы при ошибках и недопустимые данные. Браузерные сценарии проверяют поиск, чтение, возврат, прямую ссылку, режим архива, отдельные даты загрузки и публикации на desktop и iPhone WebKit. Оформление задано в [DESIGN.md](../DESIGN.md).
