# Корзина: браузер и границы доступа · 13.09.2026

## Стенд

Проверка начата на `8e46c50`, рабочая копия совпадала с веткой
`origin/codex/v3-backlog-polish`. CI 34722722661 и публикация зеркала
34753081105 завершились успешно. Локальные Web 4290, API 3039, Directus 8059
отвечали; PostgreSQL проекта `alumni-local-qa` имел статус healthy.
Это локальная проверка, а не подтверждение production.

## Подтверждено

- В браузере: каталог мерча → выбор худи M → добавление → корзина → заполнение
  синтетических контактов → отправка. Создана заявка ALU-2026-000003 на 4 200 ₽.
- PostgreSQL: статус new, связи с выпускником нет, поскольку оформление гостевое.
- API: заявка доступна администратору и отсутствует в заявках тестового выпускника.
- `/admin/orders` возвращает 401 без авторизации и с токеном выпускника;
  с токеном администратора возвращает 200. `/me/orders` без входа возвращает 401.
- Исправленный экран проверен на компьютере, при ширинах 390 и 320 px;
  на 320 px ширина документа равна ширине окна. Фокус находится на заголовке.
- 32 теста web и production-сборка web прошли.

## Исправление

Подтверждение больше не называет новую заявку находящейся в работе и не обещает
гостю наличие заявки в кабинете. После отправки фокус и прокрутка переходят
к началу подтверждения. Вошедший участник сохраняет переход к своим заявкам.

## Следующие проверки

Полный браузерный цикл вошедшего участника, отдельная роль редактора, остальные
операции админки, реальные мобильные устройства и PWA остаются отдельными этапами.
Старый контейнер `club-pravo-hse-directus-1` перезапускается; он не входит
в этот стенд и в рамках проверки не изменялся.
