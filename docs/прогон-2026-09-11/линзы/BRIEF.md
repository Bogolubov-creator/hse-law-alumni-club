# Общий бриф для линз прогона · Клуб выпускников факультета права ВШЭ · 11.09.2026

Репозиторий (только чтение, НИЧЕГО не править): /Users/buzanovsergey/hse-law-alumni-club-v2
Ветка: codex/v3-backlog-polish (HEAD 0bce454). Фронт: apps/web (Vite + React 18 + TS + Tailwind 3.4).
Живой сайт (зеркало GitHub Pages, демо-данные, без API): https://bogolubov-creator.github.io/club-pravo-hse-mirror/
Снимки: /private/tmp/claude-501/-Users-buzanovsergey/2b2d1d86-9dc4-4ae8-b693-5c9b5e192b2f/scratchpad/shots/
  desk_*.jpg – 1440px во всю высоту; mob_*.jpg – 390px (на большинстве маршрутов мобильная оболочка
  MobileApp прокручивает внутренний контейнер, поэтому снят только первый экран – это тоже стоит проверить).
Источник правды по дизайну: DESIGN.md в корне (579 строк, читать ЦЕЛИКОМ, особенно разделы с 08.09 по 11.09)
  и docs/design-system.md. Публичные поверхности: DESIGN_VARIANCE 7 / MOTION 4 / DENSITY 4; каталог 5/2/5.
  Главная «/»: живой micro-motion (решение заказчика 09.09, gpt-taste), витрина ДПО и кабинет – частотный гейт (emil).

Публичный контур (то, что прогоняем): apps/web/src/pages/{HomeV2,DpoV2,ProgramV2,MerchV2,ProductV2,CartV2,NewsV2,
EventsV2,PodcastsV2,JoinAuthV2,SupportV2,legal}.tsx, src/v2/Shell.tsx, src/mobile/MobileApp.tsx,
src/components/*, src/styles/*.css, src/index.css, index.html, public/. Кабинет /lk и админку НЕ трогаем.

Переопределения скиллов, зафиксированные заказчиком (НЕ считать нарушениями):
- en dash «–» обязателен, em dash «–» запрещён; русские кавычки «».
- Шрифты HSE Sans / HSE Slab из брендбука ВШЭ (не Geist/Inter/Satoshi), моно – системный ui-monospace.
- Tailwind 3.4, React 18, Vite; новых зависимостей и иконочных библиотек не берём.
- Никакого glassmorphism, mesh-градиентов, шума.
- Кнопки-пилюли 999px, охра #EC5A13 = цвет действия, тёмный текст на охре.

УЖЕ ИЗВЕСТНО (не повторять): на зеркале не грузятся фирменные шрифты – public/fonts/fonts.css содержит
абсолютные url('/fonts/…'), а сайт лежит в подпапке /club-pravo-hse-mirror/. Все снимки сделаны на запасных
шрифтах – НЕ оценивать выбор гарнитуры по снимкам, оценивать по коду.
Также известно: 404 на /dpo, /merch и т.п. при прямом заходе – это SPA-fallback GitHub Pages, не дефект.

Приоритеты: P1 – ломает доступность или сценарий пользователя; P2 – нарушение правила с видимым
эффектом; P3 – гигиена. Каждая находка: «P? · файл:строка · что · какое правило · как чинить (одна строка)».
Находку без файла:строки не принимаем – проверь в коде. Не выдумывай: если не уверен, пометь «(проверить)».

Формат отчёта: markdown на русском, en dash вместо em dash, до 800 слов. Сначала строка «Итог: N P1, N P2, N P3»,
затем P1, P2, P3 списками, затем «Что хорошо» (3–5 пунктов, коротко), затем «Решения владельца» – вопросы,
где скилл и DESIGN.md расходятся и выбирать должен заказчик. Отчёт записать в файл, путь указан в задании,
и вернуть в ответе только строку «Итог» и путь к файлу.
