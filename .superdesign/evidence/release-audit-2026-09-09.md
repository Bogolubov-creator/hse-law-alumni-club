# Релизный аудит · 09.09.2026

Ветка `codex/v3`. Стенд: Directus `:8255` (жив), API `:3200` (поднят для аудита), web preview на момент проверки был выключен.

## Вердикт

| Цель | Вердикт |
| --- | --- |
| **Production-релиз (полный продукт)** | **NO-GO** |
| **Локальная / закрытая демонстрация канона `/`** | **Условно GO** (по закоммиченному cutover + при выключенных внешних интеграциях) |
| **Релиз текущего WIP (~85 dirty файлов)** | **NO-GO** до ревью, коммита и полного e2e |

## Что работает (доказано сейчас)

| Проверка | Результат |
| --- | --- |
| `pnpm -C packages/shared` build + test | **71** pass |
| `pnpm -C apps/api` test | **240** pass, **13** skip (PG-gated) |
| `pnpm -C apps/web` build | OK (~2.7 с) |
| API `GET /health` | OK |
| Каталоги API (live) | programs **16**, products **3**, news **3**, events **2**, podcasts **7** |
| `GET /admin/overview` без токена | **401** (гард на месте) |
| `GET /admin/analytics` на поднятом процессе | **404** – процесс стартовал со старым `dist`; после `tsc` + рестарта маршрут есть в исходниках/unit |
| Канон маршрутов `/` | HomeV2, ДПО, мерч, новости, события, подкасты, join, ЛК, admin, legacy |
| Админ: Обзор + **Аналитика** (WIP) | API + UI есть; unit analytics/guard pass |
| Достижения (WIP) | каталог **16** в shared + Directus |

## Блокеры production (из docs + фактов)

1. **Production не деплоился и не проверялся** (`status.md`, `handoff.md`, `acceptance-cutover`).
2. **Внешние интеграции не подтверждены:** SMTP (только Mailpit), ЮKassa, Telegram bot/webhook, VAPID push (`defects.md`).
3. **152-ФЗ / оргмеры не закрыты:** оператор, РКН, локализация БД в РФ, SUPPORT в prod по умолчанию `false` (`support-and-personal-data.md`, `152fz-compliance.md`).
4. **Нет приёмки на реальном Safari/iPhone** и скринридере.
5. **Синтетический контент / нет миграции реальных ПДн**; юридические тексты помечены как проекты на локальном стенде.
6. **Большой незакоммиченный diff** (PWA, cookie, crow/FAQ, analytics UI, Vestnik, achievements 16, bot-status fix) – нельзя считать «релизом сайта» без фиксации и регрессии.

## Риски внутри кода (не внешние)

| Риск | Заметка |
| --- | --- |
| e2e `lk-v2` мокает **10** достижений | Каталог уже 16 – обновить мок/assert перед merge |
| Полный Playwright matrix | В cutover-acceptance: Deferred |
| Нет page-view аналитики | Админ-аналитика только из БД-фактов – ок для v1 офиса |
| Резерв мерча без авто-истечения | Офисная обработка (`defects.md`) |
| API dist должен пересобираться при деплое WIP | analytics в `dist` после `tsc` |

## Минимальный чеклист до GO

1. Закоммитить и отревьюить WIP (или откатить нерелизное).
2. Прод-конфиг: `APP_ENV=production`, `PUBLIC_URL=https://…`, SMTP, секреты, `SEED_DEMO=false`, `SUPPORT_ENABLED` только после проверки.
3. Sandbox ЮKassa + webhook **или** явное «заявки без оплаты» в релизе.
4. Реальный SMTP (SPF/DKIM) + тестовая доставка.
5. Safari/iPhone smoke + VoiceOver точечно.
6. Юртексты/реквизиты оператора утверждены; 152-ФЗ оргшаги по `docs/152fz-compliance.md`.
7. Полный e2e по файлам на чистой сборке + smoke admin Analytics CSV.

## Итог одной фразой

Сайт **локально функционален и собирается**, но к **публичному production-релизу не готов**: нет внешнего контура (почта/платежи/бот/push), нет оргготовности 152-ФЗ, production не выкатывался, а текущая рабочая копия ещё не зафиксирована как релизный артефакт.
