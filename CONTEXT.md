# Контекст проекта — «Клуб выпускников факультета права Вышки»

Онбординг-документ для того, кто присоединяется к проекту. Даёт картину целиком:
что это, из чего собрано, как поднять локально, где что лежит и по каким правилам
(«канону») всё сделано.

---

## 1. Что это

Веб-портал клуба выпускников факультета права Вышки. Три больших контура:

- **Публичный сайт** — главная, витрина ДПО (программы дополнительного образования),
  витрина мерча, новости, карточки программ.
- **Личный кабинет выпускника (ЛК)** — вход по e-mail/паролю, уровень и баллы
  (геймификация), достижения, персональная скидка, «мои заявки», профиль.
- **Админка учебного офиса** — верификация выпускников, начисление баллов/скидок,
  смена статусов заявок, обзор.

**Важный канон:** корзина оформляет **заявку**, с человеком связывается менеджер
учебного офиса. Скидка выпускника действует **только на ДПО**, на мерч — нет.
**Оплата** — через **ЮKassa** (yookassa.ru): контур полностью готов и включается
ключами магазина (`YOOKASSA_SHOP_ID` + `YOOKASSA_SECRET_KEY` в `.env`); без ключей
сайт работает в режиме «заявка без оплаты».

---

## 2. Стек

Монорепозиторий на **pnpm workspaces**:

| Пакет | Что | Технологии |
|---|---|---|
| `apps/web` | Фронтенд (SPA) | Vite + React + TypeScript + Tailwind, react-router, TanStack Query, zod |
| `apps/api` | Бэкенд (BFF) | Fastify + TypeScript + zod, JWT-сессии, @directus/sdk |
| `packages/shared` | Общий код | Типы, zod-схемы ответов, чистые функции (геймификация, корзина, деньги), сиды. Есть тесты (vitest) |
| `scripts` | Скрипты | `directus-bootstrap.ts` (схема/роли/сиды), `sync-hse-dpo.ts` (каталог ДПО) |
| `infra` / `docker-compose.yml` | Инфраструктура | Docker: postgres + Directus + api + web + Caddy (reverse-proxy) |

**Directus** (headless CMS) поверх **PostgreSQL** — хранилище данных и админ-панель CMS.
`apps/api` — прослойка между фронтом и Directus (наружу Directus не светим; фронт ходит
только в `/api`). Caddy проксирует `/` → web, `/api` → api.

---

## 3. Как поднять локально

Нужен **Docker** и **pnpm**.

```bash
# 1. Установить зависимости
pnpm install

# 2. Создать .env из примера и заполнить секреты (см. .env.example)
cp .env.example .env
#   ВАЖНО: .env в гите нет (он в .gitignore). Секреты каждый заводит у себя.
#   Минимум: пароли БД/админа, AUTH_SECRET (>=32 симв.), DIRECTUS_SERVICE_TOKEN.

# 3. Поднять весь стек (соберёт образы и прогонит bootstrap-сиды)
docker compose up -d --build
```

Адреса после запуска:

| Что | URL |
|---|---|
| Сайт | http://localhost |
| Личный кабинет | http://localhost/lk |
| Админка офиса | http://localhost/#/admin |
| API health / ready | http://localhost/api/health · http://localhost/api/ready |
| Directus (CMS-админка) | http://localhost:8055 |

`bootstrap` — одноразовый сервис: создаёт коллекции/поля/роли, сервисный токен, сиды
(уровни, правила баллов, достижения, программы, товары, новости) и тестовые аккаунты.
Идемпотентен — повторный запуск ничего не ломает.

### Грабли окружения (важно)
- **Кириллица в пути ломает Docker BuildKit.** На macOS папка проекта называется
  кириллицей — используется ASCII-симлинк, а сборка идёт с `COMPOSE_BAKE=false`.
  Если поднимаешь в кириллическом пути и падает BuildKit — сделай симлинк на латинице
  и запускай docker-команды через него.
- **Directus не принимает e-mail с TLD `.local`.** Тестовые аккаунты — на `@club.example.com`.

---

## 4. Ключевые механики

### Геймификация (единый источник — `packages/shared/src/gamification.ts`)
- **Баллы** пишутся в `points_ledger` (источник правды) → пересчитывается кэш
  `points_cached` / уровень.
- **Уровни:** Выпускник (5%) → Друг клуба (10%) → Знаток (15%) → Амбассадор (20%).
- **Скидка** = `min(база уровня + clamp(personal, 0..10), 25)` — справочная (оплаты нет).
- **Достижения** — 10 штук, набор и оформление повторяют дизайн (Claude Design):
  ромбы teal→navy (получено) / оранжевый (следующее) / soft (закрыто), символы ✓@♥✎★.
  Прогресс считается в коде (`achievementProgress`) из статистики выпускника.
- **Decay** — −15% баллов за месяц неактивности (cron, идемпотентно по месяцу).

### Каталог ДПО — живая автосинхронизация с hse.ru
Каталог автоматически забирается с сайта ВШЭ (факультет права, `orgUnit=22753`):
https://www.hse.ru/edu/dpo/?orgUnit=22753 (переопределяется env `HSE_DPO_URL`).

Как устроено:
- **Парсер** — `parseHseDpoCards` в `packages/shared/src/hse-dpo.ts` (карточки
  `dpob-card`: название, ПК/ПП, формат, дата старта, длительность, цена; покрыт тестами).
- **Движок** — `apps/api/src/lib/hse-sync.ts`: сопоставление по `source_url` (id
  программы на hse.ru), затем по нормализованному названию; обновляются цена/формат/
  старт/длительность/документ; **ручные поля (описание, модули, направление) не
  затираются**; пропавшие с сайта программы → «Архив» (только управляемые синком —
  добавленные вручную через админку не трогаются); < 3 карточек в парсе → синк
  отменяется (защита от смены вёрстки).
- **Запуск**: ночной cron в 05:00 (apps/api) · кнопка **«⟳ Обновить с hse.ru»**
  в админке (Контент → Программы ДПО) · вручную с хоста:
```bash
DIRECTUS_URL=http://localhost:8055 pnpm --filter @club/scripts exec tsx src/sync-hse-dpo.ts
```

### Сообщество и интересы
- **«Сообщество» в ЛК** — «найти своих»: список верифицированных однокурсников того же
  выпуска (cohort) и/или той же образовательной программы (edu_program) с кнопкой
  «В друзья». Связи — коллекция `alumni_friends` (pending → accepted; встречная заявка
  автоматически принимает дружбу). API: `GET /me/classmates`, `POST /me/friends`.
- **Интересы в юриспруденции** — справочник `LEGAL_INTERESTS` (@club/shared/interests),
  выпускник выбирает до 8 чипов в Профиле (`alumni.interests_json`); ввод фильтруется
  строго по справочнику (`sanitizeInterests`). Интересы видны однокурсникам в «Сообществе».
  При появлении самостоятельной регистрации — включить выбор интересов в онбординг.

### Оплата (ЮKassa)
- `apps/api/src/lib/yookassa.ts` — создание платежа (redirect, capture, чек 54-ФЗ при
  включённой фискализации) и верификация статуса; идемпотентность через Idempotence-Key.
- Роуты `apps/api/src/routes/payments.ts`: `GET /payments/config` (флаг для фронта),
  `POST /orders/:number/pay` (ссылка на оплату — только владельцу заявки),
  `POST /payments/yookassa/webhook` — **телу вебхука не доверяем**, статус перепроверяется
  прямым запросом к API ЮKassa; `payment.succeeded` → заявка `confirmed` + `paid_at`.
- При оформлении заявки платёж создаётся сразу, ссылка приходит в ответе (`payment_url`)
  и показывается кнопкой на экране «Заявка отправлена». Сбой оплаты не роняет заявку.
- Поля заказа: `payment_id`, `payment_status`, `paid_at`.
- **Включение:** в кабинете ЮKassa получить `shopId`/секретный ключ → в `.env`:
  `YOOKASSA_SHOP_ID`, `YOOKASSA_SECRET_KEY`, `PUBLIC_URL=https://домен`; в кабинете ЮKassa
  указать webhook `https://домен/api/payments/yookassa/webhook` (события payment.succeeded,
  payment.canceled) и включить фискализацию, если чеки формирует ЮKassa.

### 152-ФЗ (персональные данные)
Встроено в код:
- Страницы: `/privacy` (Политика обработки ПДн – цели, категории, основания, субъекты,
  порядок/сроки, cookies, права), `/confidential` (Политика конфиденциальности),
  `/requisites` (владелец: наименование, ОГРН, ИНН, КПП, адрес, руководитель). Файл –
  `apps/web/src/pages/legal.tsx`; реквизиты – `CLUB_OPERATOR` в `packages/shared/src/operator.ts`
  (сверка https://www.rusprofile.ru/id/1257700005551).
- Ссылки на политику + краткие реквизиты владельца – в футере **всех** страниц (шрифт ≥12px).
- **Cookie-баннер** при первом визите (кнопка «Принять», `localStorage club_cookie_consent`).
- Согласия: у формы заявки – активный чекбокс (пустой по умолчанию) со ссылкой на политику
  и наименованием оператора; у логина и сохранения профиля – явные пометки со ссылкой.
- Платёжные данные карт не собираются (вводятся на стороне ЮKassa).

**Чеклист прод-деплоя (152-ФЗ + безопасность):**
1. **Сервер в РФ** — VPS/хостинг физически в России (локализация БД, ст. 18 ч. 5 152-ФЗ).
   Иностранные CRM/аналитика без локализации — нельзя.
2. **HTTPS** — Caddy получает сертификат Let's Encrypt автоматически при реальном домене
   (заменить `:80` на домен в Caddyfile). Для интернет-магазина рассмотреть OV-сертификат.
3. **Реквизиты** – сверить `CLUB_OPERATOR` в `packages/shared/src/operator.ts` и футерах
   с https://www.rusprofile.ru/id/1257700005551 (включая сведения о лицензии при необходимости).
4. **Роскомнадзор** – подать уведомление об обработке ПДн (форма на сайте РКН).
5. **Бэкапы** – pg_dump минимум раз в сутки + хранение вне сервера.
6. **Обновления** — регулярно обновлять Directus/зависимости (`pnpm audit`).
7. **DDoS/WAF** — включить защиту на стороне провайдера (напр. DDoS-Guard/CloudMTS).
8. Мониторинг и журналирование доступа уже ведутся (fastify logger, Directus activity).

### Безопасность
rate-limit (глобальный 300/мин; auth 5/мин; friends/profile/pay — свои лимиты), helmet,
CORS-allowlist, bodyLimit, trustProxy; собственные JWT-сессии
(`AUTH_SECRET`, 7 дней для выпускника; отдельный `ADMIN_AUTH_SECRET`, 12 ч, scope=admin);
UUID-сессии корзины; рантайм-валидация ответов фронта через zod-схемы из `@club/shared`;
анти-подмена цены (reprice по каталогу при оформлении); XSS — React-экранирование,
SQL-инъекции — доступ к БД только через Directus API.

**Шифрование и защита контента/данных:**
- **Аудио подкастов** — реальные URL никогда не покидают сервер: клиент получает
  подписанную ссылку `/api/podcasts/:id/audio?exp&sig` (HMAC-SHA256 от `id.exp`
  ключом `AUTH_SECRET`, TTL 6 ч, сравнение `timingSafeEqual`); проверка → 302 на
  источник. Подделка/истечение → 403.
- **Бэкапы БД** — `scripts/backup-db.sh`: pg_dump → gzip → AES-256-CBC
  (PBKDF2, 200k итераций), ключ `BACKUP_ENCRYPTION_KEY` в `.env` (хранить отдельно
  от бэкапов!), ротация 14 дней, `backups/` в .gitignore. Рецепт восстановления —
  в шапке скрипта. На проде — в cron ежесуточно + выгрузка на внешнее хранилище в РФ.
- **TLS** — Caddy: авто-сертификаты Let's Encrypt на реальном домене; HSTS.
- **HTTP-заголовки (Caddy)** — HSTS, nosniff, Referrer-Policy, CSP frame-ancestors,
  Permissions-Policy (камера/микрофон/геолокация/платежи выключены), `-Server`.
- **Пароли** — хешируются Directus (argon2); карты не касаются нашего сервера (ЮKassa).
- Directus (8055) и Postgres наружу не публикуются (bind 127.0.0.1 / только внутренняя сеть).

**Защита от атак, DDoS и ботов:**
- **Rate-limit** per-IP: глобально 300/мин (health в allowlist, `continueExceeding`);
  жёстче на дорогих/чувствительных ручках — auth 5/мин, заявка `/orders` 6/мин,
  оплата/подписка 5–10/мин, друзья/профиль 20/мин, classmates 30/мин, `/cart` 40/мин.
- **Анти-брутфорс по аккаунту** (`lib/security.ts`): 10 неудачных логинов на e-mail →
  блок на 15 мин независимо от IP (защита от распределённого перебора); поверх per-IP.
- **Аудит-след** (`audit_log`, `lib/audit.ts`): логины (ok/fail/locked), создание заявок,
  платежи (succeeded/canceled/bad-ip), смены статусов, верификация, выдача подписок —
  с actor и IP; append-only, для расследования инцидентов и споров по оплате.
- **Вебхук ЮKassa** — двойная защита: IP-allowlist официальных подсетей ЮKassa
  (`isYookassaIp`) + верификация статуса прямым запросом к API (телу не верим).
- **Honeypot** на форме заявки: скрытое поле `website` (offscreen, aria-hidden);
  заполнено ботом → 400, заявка не создаётся, офис не спамится.
- **Caddy-периметр**: таймауты (read_header 5s, read_body 10s, idle 60s — анти-Slowloris),
  `max_header_size 16kb`, `request_body max_size 512kb`; ловушка сканеров — типовые
  «дыры» чужих CMS (`/wp-login.php`, `/.env`, `/.git/*`, `/xmlrpc.php`, …) → 404, не доходя
  до приложения. На проде дополнительно — DDoS-защита провайдера/CDN (см. чеклист).
- **JWT** — верификация с пином `algorithms: ["HS256"]` (защита от alg-confusion).
- **Идемпотентность платежа** — ключ ЮKassa детерминирован по номеру заявки
  (ретрай/двойной клик не создаёт дубль оплаты).
- **Аудио подписчику** — подпись привязана к его alumni-id; при отдаче платного выпуска
  повторно проверяется активная подписка (перепродажа ссылки бесполезна).
- **no-store** на персональных/платёжных ответах; `no-new-privileges` у всех контейнеров;
  предупреждения в логах при небезопасной прод-конфигурации (payment без https, пустой
  ADMIN_AUTH_SECRET). Ограничения размеров/значений на входах (длины строк, delta баллов ±2000).

---

## 5. Структура репозитория

```
apps/
  web/   src/pages/       # Home, Dpo, Program, Merch, Cart, News, Lk, Profile
         src/admin/       # AdminApp (админка офиса)
         src/components/  # SiteShell, Modal, Toast
         src/lib/         # api (клиент /api), cart, queries
  api/   src/routes/      # content, me, orders, cart, auth, admin, ...
         src/lib/         # directus (типизированная схема), auth, engine (геймификация), notify
packages/shared/src/      # gamification, cart, order-calc, order, responses (zod), seeds
scripts/src/              # directus-bootstrap, sync-hse-dpo
design-export/            # исходные макеты Claude Design (*.dc.html) — источник правды по UI
docs/                     # directus-schema.md и пр.
docker-compose.yml, infra/ Caddyfile
```

**Дизайн-макеты** лежат в `design-export/*.dc.html` (Главная, Дашборд ЛК, Профиль,
Витрина ДПО, Карточка программы, Витрина мерча, Корзина и заказ, Дизайн-кит, Админка) —
это источник правды по внешнему виду; реализация сверялась с ними.

**Палитра/шрифты (канон):** ohra `#EC5A13`, hse-blue `#11296B`, grafit `#14181F`,
kost `#FBF3E8`; шрифты Unbounded (заголовки) / Onest (текст) / Martian Mono (моно/цифры).
Тире — только среднее «–»/«—», не дефис.

---

## 6. Разработка

```bash
pnpm --filter @club/shared build      # собрать общий пакет (типы для api/web)
pnpm --filter @club/shared exec vitest run   # тесты общей логики
pnpm --filter @club/api build         # typecheck api
pnpm --filter @club/web build         # typecheck + сборка фронта
pnpm --filter @club/web dev           # дев-сервер фронта (Vite)
pnpm --filter @club/api dev           # дев-сервер api
```

Общая логика денег/корзины/геймификации вынесена в чистые функции в `@club/shared` и
покрыта тестами — правь бизнес-правила там, а не в роутах.

---

## 7. Текущее состояние

MVP собран и проходил многоагентное ревью (безопасность, типы, тесты, UX) и сверку с
дизайн-макетами. Реализованы витрины, ЛК с геймификацией, корзина-заявка (без оплаты),
админка офиса, mini-app-ядро (Telegram initData + рефералка).

**Открыто / требует внешних секретов:** боевые токены уведомлений
(`OFFICE_TG_BOT_TOKEN`, `SMTP_*`) и `TELEGRAM_BOT_TOKEN` для живого входа mini-app —
код готов, без токенов уходит в лог. Продовый деплой и Lighthouse/a11y-аудит на проде.

Осознанно отложено (по продуктовому решению): загрузка аватара, тёмная тема,
3-шаговый мастер оформления корзины, in-app CRUD в админке (сейчас → Directus Studio),
цветовой селектор мерча.

**Overnight Phase 6 (11.09.2026) – хвосты:** банковские реквизиты TBD (нет в
Rusprofile); фото `graduate-robe` отсутствует; e2e live `/programs` нуждается в
здоровом Directus; опционально – chip-фильтры ДПО; Admin JWT всё ещё в
`localStorage`; A2HS на реальном устройстве – вручную. Зеркало Pages:
https://bogolubov-creator.github.io/club-pravo-hse-mirror/ (`docs/pages-mirror.md`).

