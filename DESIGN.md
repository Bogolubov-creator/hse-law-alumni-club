# DESIGN.md · Клуб выпускников факультета права НИУ ВШЭ

Действующие дизайн-решения v2. Этот файл – источник правды для скиллов и для кода.
Подробное обоснование и отвергнутые варианты – в [DESIGN-PLAN.md](./DESIGN-PLAN.md).

> Файл создан один раз и дополняется. Скиллы `stitch-design-taste` и `impeccable`
> оба умеют генерировать DESIGN.md – **перезаписывать этот файл нельзя**, только
> дописывать разделы.

---

## Решения, принятые заказчиком (2026-08-03)

| Вопрос | Решение |
|---|---|
| Главная | **Вариант A «Реестр»** – горизонтальные записи с моноширинной колонкой слева вместо сетки одинаковых карточек |
| Главная кнопка сайта | **Тёмный текст на охре** (5,12:1, WCAG AA) вместо светлого (3,1:1) |
| Сигнатура «поле бланка» | **Принята как есть** – только там, где под линией настоящие данные реестра |
| Следующая поверхность | **Витрины ДПО и мерча** |

---

## Дилы по поверхностям

Значения выбираются под поверхность, глобальных потолков нет.

| | Внешний контур | Личный кабинет |
|---|---|---|
| Экраны | `/`, витрины, `/join`, новости, события | `/lk`, `/lk/profile`, админка |
| Задача | объяснить и довести до заявки | быстро и без сюрпризов |
| DESIGN_VARIANCE | 7 | 3 |
| MOTION_INTENSITY | 4 – один оркестрованный момент | 1 – только состояния |
| VISUAL_DENSITY | 4 | 7 |

Режим редизайна: **preserve**. IA, маршруты, контракты API и голос копирайта сохраняются.

---

## Токены

Два слоя, файл `apps/web/src/styles/tokens.css`.

- **Слой 1 – палитра.** Единственное место в проекте, где встречается hex.
  Канон (`--p-ohra`, `--p-blue`) неприкосновенен; расширение взято из дизайн-кита.
- **Слой 2 – семантика.** Компонент просит `--c-accent-text`, а не «охру».
  Тёмная тема – подмена значений этого слоя, а не правка компонентов.

Компонентам запрещено обращаться к слою 1 напрямую.

**Статусные цвета разделены на заливку и текст.** `--c-ok` / `--c-danger` –
только заливки; для текста берутся `--c-ok-text` / `--c-danger-text`. Причина
измерена: базовый зелёный даёт 3,94:1 на кости и 4,11:1 на графите, кармин –
2,93:1 на графите. На мелкой моно-подписи это провал AA, поэтому у текстовых
токенов свои тона и свои значения в тёмной теме.

**Типографика:** Unbounded (display), Onest (body), Martian Mono (данные).
Шкала в токенах с `clamp`. Моноширинный – всё, что является данными реестра,
с `tabular-nums`, чтобы числа не прыгали при обновлении.

---

## Сигнатура: «поле бланка»

Тонкая линия с моноширинной подписью под ней – типографика официального бланка.
Класс `.blank-field` в `tokens.css`.

**Правило:** появляется только там, где под ней настоящие данные из реестра.
Это не разделитель и не украшение. Если данных нет – элемента нет.

---

## Движение

Один оркестрованный момент на страницу, не россыпь.

- **Внешний контур:** сборка героя при загрузке – 300 мс, стагер 40–60 мс,
  `--ease-out`. Больше ничего не анимируется, кроме состояний.
- **Кабинет:** движения нет, только `:active` и переходы состояний.
- Анимируются только `transform` и `opacity`.
- `prefers-reduced-motion` выключает сборку и маркизу целиком.

**Разрешение конфликта скиллов:** `gpt-taste` требует «perpetual micro-motion»,
`emil-design-eng` – частотный гейт (чем чаще действие, тем меньше анимации).
Принят **emil-design-eng**: совпадает с брифом. Конфликт не выбирается молча –
если поверхность потребует иного, решение фиксируется здесь.

---

## Переопределения скиллов (бриф побеждает)

| Скилл | Что требует | Решение |
|---|---|---|
| `design-taste-frontend` §9.G | запрет en dash как разделителя | **Отменено.** Стандарт проекта: en dash обязателен, em dash запрещён полностью |
| `design-taste-frontend` §4.1 | Geist / Satoshi / Outfit | **Отменено.** Канон: Unbounded / Onest / Martian Mono |
| `design-taste-frontend` §3.A | Tailwind v4, Motion, Next.js | **Отменено.** Проект на Tailwind 3.4, React 18, Vite |
| `design-taste-frontend` §3.C | иконки только из библиотек | **Отменено.** Новых зависимостей не берём, иконки инлайн-SVG |
| `redesign-existing-projects` | glassmorphism, mesh-градиенты, шум | **Отменено.** Прямой анти-паттерн брифа |

---

## Что уже сделано

- `apps/web/src/styles/tokens.css` – система токенов, обе темы, сигнатура.
- `apps/web/src/pages/HomeV2.tsx` – главная, вариант A, маршрут `/v2`.
  Старая главная на `/` не тронута, чтобы можно было сравнить.
- `apps/web/src/v2/Shell.tsx` – общая шапка и подвал витрин.
- `apps/web/src/pages/DpoV2.tsx`, `MerchV2.tsx` – витрины, маршруты `/v2/dpo`, `/v2/merch`.
- `apps/web/src/pages/LkV2.tsx` – кабинет, маршрут `/v2/lk`. Режим кабинета:
  плотность 7, движения нет, один акцент на все действия. Карточка выпускника
  собрана как удостоверение – сигнатура «поле бланка» стоит там, где под чертой
  действительно данные реестра (выпуск и образовательная программа).
- `apps/web/src/v2/cabinet.tsx` – общие примитивы приватной зоны: шапка с
  вкладками, подпись реестра, действие, полоса прогресса. Кабинет и профиль
  берут их отсюда, чтобы не разойтись при первой же правке.
- `apps/web/src/pages/ProfileV2.tsx` – профиль, маршрут `/v2/lk/profile`.
  Форма собрана как опись: подпись реестра над полем, разделитель – линия.
  Перенесён весь функционал v1, включая права по 152-ФЗ.
- `apps/web/e2e/lk-v2.spec.ts`, `profile-v2.spec.ts` – приватная зона
  проверяется на подменённых ответах API: настоящие учётные данные в тесты не
  кладём, а отрисовку, пустые состояния, ошибку и истёкшую сессию проверить
  надо. Фикстуры выключают service worker: мобильный проект бежит на WebKit,
  где запросы через активный SW до `page.route()` не доходят.

## Что дальше

- Мобильная оболочка в языке v2 (сейчас на телефоне `/v2/*` отдаёт те же
  страницы адаптивно, а `MOBILE_APP_ROUTES` перехватывает только маршруты v1).
- Корзина и оформление заявки в языке v2 – последний экран, который из v2
  роняет в старый интерфейс.
